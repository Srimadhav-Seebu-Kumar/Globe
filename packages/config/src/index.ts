export const PLATFORM_NAME = "Globe Land Intelligence";

export const API_PREFIX = "/v1";

export const DEFAULT_MARKET_ROLLOUT_RULES = {
  requireLegalDisplayPolicy: true,
  requireSourceRegistryEntries: true,
  requireCoverageSnapshot: true
} as const;

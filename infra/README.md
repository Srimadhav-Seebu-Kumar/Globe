# Infrastructure Scaffold

Local infrastructure defaults for development:
- Postgres + PostGIS as source of truth
- Redis for caching and queues
- OpenSearch for full-text and geo search
- MinIO for S3-compatible object storage

Use `docker compose up -d` from this directory.
